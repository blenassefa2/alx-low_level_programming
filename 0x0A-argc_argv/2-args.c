#include <stdio.h>
/**
 * main - prints all arguments passed to it
 *
 * Return - 0 always (success)
 */
int main (int argc, char *argv[])
{
	int x;

	for (x = 0; x < argc; x++)
		printf("%s\n", s[x]);
	return (0);
}
